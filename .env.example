# Copy to .env (and/or keep VITE_* in .env.local). Never commit real secrets.

# Supabase CLI (migrations during deploy). Create at:
# https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=

# Optional override; otherwise derived from VITE_SUPABASE_URL host.
# SUPABASE_PROJECT_REF=

# Vite / browser (required for production build)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App is served at the root via the custom domain (looms.gg), so no VITE_BASE is
# needed. Set VITE_BASE=/looms-web/ only if serving from project Pages.
# VITE_BASE=/looms-web/

# Cloudflare Turnstile site key (public); secret lives in Supabase Bot Protection
VITE_TURNSTILE_SITE_KEY=
